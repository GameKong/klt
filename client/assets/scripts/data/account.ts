import { GConst } from "../globals/gConst"

export class Account {
    public static username: string
    public static password: string
    public static ssoToken: string
    public static area_number: number = GConst.InValidServer
    
}

